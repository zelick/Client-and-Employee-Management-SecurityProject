<configuration>
    <!-- Console Appender -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%-30(%d{yyyy-MM-dd HH:mm:ss.SSS} %p) %-30.30([%t] %C): eventId=%X{eventId} - %msg%n%throwable</Pattern>
        </layout>
    </appender>

    <!-- File Appender -->
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app-logback.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app-logback.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %p [%t] %C: eventId=%X{eventId} - %msg%n%throwable</Pattern>
        </encoder>
    </appender>

    <!-- Hikari Logger -->
<!--    <logger name="com.zaxxer.hikari" level="DEBUG">-->
<!--        <appender-ref ref="File" />-->
<!--    </logger>-->

    <logger name="org.example" level="DEBUG"/>

    <root level="DEBUG">
        <appender-ref ref="Console" />
        <appender-ref ref="File" />
    </root>
</configuration>
